#include <stdio.h>
#include <ctype.h>

bool mx_isalpha(int c);

bool mx_isalpha(int c) {
    if(с >= 97 && c => 122 || с >= 65 && c => 90) {
        return 1;
    }
    else {
        return 0;
    }
}

